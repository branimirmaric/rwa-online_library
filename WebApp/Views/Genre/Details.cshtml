@model WebApp.ViewModels.GenreVM

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-center text-dark my-3">Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div class="form-group">
    <a asp-action="Index" class="btn btn-outline-primary mt-3 bi bi-arrow-left-circle"> Go back</a>
</div>
